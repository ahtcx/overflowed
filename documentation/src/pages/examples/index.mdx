---
title: Examples
layout: ~/layouts/Documentation.astro
---

import FrameworkTabs from "../../components/tabs/FrameworkTabs.astro";
import Playground from "../../components/Playground.astro";

This page contains various uses of Overflowed and it's implementation in each framework. You can
resize the container by dragging the handle on the right side of each example.

## Toolbar

This example shows how an application toolbar could be implemented.

import { ToolbarExample } from "./_ToolbarExample.tsx";
import toolbarExampleSource from "./_ToolbarExample.tsx?raw";

<Playground source={toolbarExampleSource} lang="tsx">
	<ToolbarExample client:load />
</Playground>

## Avatar groups

This example shows a live list of GitHub stargazers as avatars. The end
indicator shows how many avatars are currently hidden. Feel free to [add
yourself 🥰](https://github.com/ahtcx/overflowed/stargazers).

import { AvatarExample as AvatarExampleReact } from "./_AvatarExample.tsx";
import avatarExampleReactSource from "./_AvatarExample.tsx?raw";

<Playground source={avatarExampleReactSource} lang="tsx">
	<AvatarExampleReact client:load />
</Playground>

## Navigation bar

In this example the navigation bar has too many items to fit on a single line, so we add the overflowing options into a dropdown menu.

import { NavigationExample as NavigationExampleReact } from "./_NavigationExample.tsx";
import navigationExampleReactSource from "./_NavigationExample.tsx?raw";

<Playground source={navigationExampleReactSource} lang="tsx">
	<NavigationExampleReact client:load />
</Playground>

## Transitions

This examples showcases items fading in/out and the indicator smoothly moving around. This is simply
acheived with CSS and allows you write styling like you normally would.

import { PlaylistExample as PlaylistExampleReact } from "./_PlaylistExample.tsx";
import playlistExampleReactSource from "./_PlaylistExample.tsx?raw";

<Playground source={playlistExampleReactSource} lang="tsx">
	<PlaylistExampleReact client:load />
</Playground>

## Animations

This example demonstrates Overflowed's ability to adapt to lists with moving or animated elements.
The letters are constantly resizing and the library is able to keep up with these changes at 60FPS.

TODO: improve performance
